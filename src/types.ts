export interface User {
  email: string
  token: string
  username: string
  createdAt: string
}

export interface Contact {
  id: string
  createdAt: string
  username: string
  contactname: string
  contactemail: string
}
